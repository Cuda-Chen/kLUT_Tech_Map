#include <iostream>
#include <string>
#include <fstream>

#include "mapper.hpp"

using namespace std;

void mapper_foo()
{
    cout << "mapper foo" << endl;
}

void mapper(string inputFile, string outputFile)
{
}

void mapperCheater(std::string inputFile, std::strint outputFile)
{
    // first use random to determine the circuit is valid
    // 50% 50%

    // then randomly choose some PIs of k-LUT

    // then randomly choose some POs of k-LUT
}
